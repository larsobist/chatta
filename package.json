{
  "name": "chatta",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run backend --prefix backend\" \"npm run frontend --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
